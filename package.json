{
  "name": "phil-onion-watch",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@9.12.2",
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "format": "pnpm -r format",
    "smoke:compose": "node scripts/smoke-compose.mjs"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
